# RAG Engine Requirements

# FastAPI
fastapi==0.110.0
uvicorn[standard]==0.27.1

# LLM Clients
openai==1.12.0
anthropic==0.18.0
tiktoken==0.6.0

# HTTP Client
httpx==0.26.0

# 工具
pydantic==2.6.1
pydantic-settings==2.1.0  # For BaseSettings in Pydantic v2
python-multipart==0.0.9

# 监控
prometheus-client==0.19.0
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-exporter-otlp==1.22.0

# 日志
loguru==0.7.2

# Re-ranking and Hybrid Search
sentence-transformers==2.5.1  # Cross-Encoder for re-ranking
rank-bm25==0.2.2  # BM25 algorithm

# FAISS for fast similarity search (Iter 1.3)
faiss-cpu==1.7.4  # or faiss-gpu for GPU support

# Graph processing (Iter 2)
networkx==3.2  # Lightweight graph library
# neo4j==5.15.0  # Optional: for enterprise graph database

# Redis for caching
redis==5.0.1

# NLI model for hallucination detection (Iter 3)
# transformers==4.36.0  # Uncomment for Self-RAG

# Context compression (Iter 3)
# llmlingua==0.2.0  # Uncomment for context compression
